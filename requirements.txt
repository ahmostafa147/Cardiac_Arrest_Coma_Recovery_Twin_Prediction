# Pinned to the versions this pipeline was actually verified on.
# An unpinned install currently resolves to plotly 7 and pandas 3 — both major
# bumps that were never tested here, and plotly 7 is the risk: the figures use
# multiple named legends and 3-D scene APIs that changed across that boundary.

numpy==1.26.4
scipy==1.16.3
scikit-learn==1.7.2
torch==2.11.0
cebra==0.6.0
plotly==6.5.0
kaleido==1.3.0
tqdm==4.67.1

# only needed by scripts/06_embedding_eval.py
catboost==1.2.8
matplotlib==3.10.7
pandas==2.3.3
